string[] frame_ids
bool[] updatetf # if updatetf.length != frame_ids.length update[0] is used for all
float64[] axis_length # [m] if axis_length.length != frame_ids.length axis_length[0] is used for all
float64[] axis_radius # [m] also used as size of origin point; if axis_radius.length != frame_ids.length axis_radius[0] is used for all
